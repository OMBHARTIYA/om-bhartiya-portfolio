{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "generate:cv": "node scripts/generate-cv.mjs",
    "prebuild": "npm run generate:cv",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "pdfkit": "0.19.1",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
